import { createReducer } from "@reduxjs/toolkit";
import { filterByString } from "../actions";

const initialState = '';

// const searchReducer = createReducer(initialState, (builder) => {
//   builder
//     .addCase(filterByString, (state, action) => {
//       state = action.payload
//     })
// });

